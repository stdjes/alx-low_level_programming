#include "main.h"
/**
 *add - adds two numbers and returs the sum
 *@a: first integer variable
 *@b: second interger variable
 *
 * Return: sum of the intergers
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
