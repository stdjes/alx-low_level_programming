#include "main.h"
#include <stdio.h>

/**
 * _strlen - function that returns the lenght of a string
 *@s: variable for string array
 *
 * Return: the cummulative lenght of string counted by variable l
 */
int _strlen(char *s)
{
	int l; /* length counter */

	for (l = 0; s[l] != '\0'; l++)
	{

	}

	return (l);
}
