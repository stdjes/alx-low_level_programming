#include "main.h"

/**
 * mul - computes the product of two integers
 * @a: first integer variable
 * @b: second integer variable
 *
 * Return: the value ot the product of the two integers
 */
int mul(int a, int b)
{
	int product = 0;

	product = a * b;

	return (product);
}
