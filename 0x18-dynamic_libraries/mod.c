#include "calc.h"

/**
 * mod - Subtracts two given numbers.
 * @a: First number.
 * @b: Second number.
 *
 * Return: the remainder of 'a' and 'b'.
 */
int mod(int a, int b)
{
        return (a % b);
}
